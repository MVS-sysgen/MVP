[DEFAULT]
hercules_ip = 127.0.0.1
ascii_reader = 3505
ebcdic_reader = 3506
user = MVP
desc = desc
packages = packages
cache = cache
MVP_repo = https://github.com/MVS-sysgen/MVP
# No password here on purpose. INSTALL_MVP generates a random one and
# writes it back to this file, so every installation gets its own. A
# shipped value is a well-known default on every MVS/CE system, and the
# MVP account is DFLTGRP(ADMIN) GROUP(RAKFADM) OPERATIONS.
